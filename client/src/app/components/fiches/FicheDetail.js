import { default as React } from 'react';

import './fiche.scss'

const FicheDetail = ({  }) => {
  return (
    <div className="fiche-detail">
		fiche detail
    </div>
  );
};

export default FicheDetail;

